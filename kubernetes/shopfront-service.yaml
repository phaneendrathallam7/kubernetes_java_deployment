---
apiVersion: v1
kind: Service
metadata:
  name: shopfront1
  labels:
    app: shopfront1
spec:
  type: NodePort
  selector:
    app: shopfront1
  ports:
  - protocol: TCP
    port: 8010
    name: http

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopfront1
spec:
  selector:
    matchLabels:
      app: shopfront1
  replicas: 1
  template:
    metadata:
      labels:
        app: shopfront1
    spec:
      containers:
      - name: shopfront1
        image: phaneendrat7/shopfront:1.0.0
        ports:
        - containerPort: 8010
        livenessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 30
          timeoutSeconds: 1
